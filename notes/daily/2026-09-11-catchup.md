# Catchup — 2026-09-11

## Completed Today
- Added a `test_cases` SQLite table and full CRUD API (`server/db.js`, `server/test-cases.js`), seeded with 5 sample test cases
- Built the `/test-cases` page: sortable/filterable table, pagination, create/edit form modal, read-only details modal, delete confirmation modal
- Added severity and test-type pill badges with distinct colors
- Fixed a step-numbering duplication bug in the details view
- Audited the feature against `CLAUDE.md` and fixed the API response envelope so malformed requests and unmatched routes also return `{success, data, error}` instead of raw HTML errors
- Manually tested the delete flow by creating and deleting a test case in the UI

## Blockers / Risks
- None

## Focus for Tomorrow
- Continuing with course material; next project tasks to be defined afterward
